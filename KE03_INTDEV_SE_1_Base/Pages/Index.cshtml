@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}
<link rel='stylesheet' type='text/css' media='screen' href='~/css/index.css'>
<div>
    <h2 class="title">Meest verkochte producten</h2>
    <div class="bestSellingContainer">
        @foreach(var p in Model.BestSellingProducts)
        {
            <div class="productContainer">
                <div><img src="@p.ImageUrl" class="productImg"/></div>
                <div class="productAnouncement">
                    <p class="productTitle">@Html.DisplayName(p.Name)</p>
                    <p class="productPrice">@Html.DisplayName("€" + p.Price.ToString())</p>
                </div>
            </div>
        }
    </div>
</div>











@* <table class="table">
    <thead>
        <tr>
            <th>Product</th>
            <th>Descriptie</th>
            <th>Prijs</th>
        </tr>
    </thead>
    @foreach (var p in Model.BestSellingProducts)
    {
        <tr>
            <td><img src="@p.ImageUrl" alt="@p.Name" class="img-thumbnail" /></td>
            <td>@Html.DisplayName(p.Name)</td>
            <td>@Html.DisplayName(p.Description)</td>
            <td>@Html.DisplayName("$" + p.Price.ToString())</td>
        </tr>
    }
</table> *@